{
  "name": "eigenstate",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=24.5.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "gen:og": "node scripts/build-og.mjs",
    "build": "npm run gen:og && vue-tsc --noEmit && vite build && node scripts/build-sw.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "vue-tsc --noEmit",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": { "vue": "^3.5.13" },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@vitejs/plugin-vue": "^6.0.8",
    "fake-indexeddb": "^6.2.4",
    "sharp": "^0.35.4",
    "typescript": "^5.7.2",
    "vite": "^8.2.0",
    "vue-tsc": "^3.3.9",
    "wrangler": "^4.118.0"
  }
}
